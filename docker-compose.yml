version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
      - DB_HOST=dpg-d6ikbp5pdvs73fc5pig-a
      - DB_PORT=5432
      - DB_NAME=iskele360_db_v8
      - DB_USER=iskele360_db_v8_user
      - DB_PASS=K9iWuJiLfXTDyT7tAfdSLWsRWCyXmaUw
    volumes:
      - .:/app
      - /app/node_modules
    container_name: iskele360-backend-v8
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:8080/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    volumes:
      - ./logs:/usr/src/app/logs
    networks:
      - iskele360-network

networks:
  iskele360-network:
    driver: bridge 